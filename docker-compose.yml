version: '3.8'

services:
  web:
    build: .
    container_name: articulo65_web
    environment:
      - DEBUG=False
      - SECRET_KEY=django-insecure-docker-key-change-in-production
      - DB_NAME=test2
      - DB_USER=maquio
      - DB_PASSWORD=maquio92
      - DB_HOST=172.16.35.75
      - DB_PORT=32768
    volumes:
      - media_files:/app/media
      - static_files:/app/staticfiles
    ports:
      - "3018:8000"
    restart: unless-stopped
    network_mode: host

volumes:
  media_files:
  static_files: